# Data.gov.hk 真实数据手动下载指南

## 📋 当前状态

**问题**: 自动化下载URL返回404错误 (使用示例resource_id)
**解决**: 需要手动获取真实下载链接

---

## 🚀 立即手动下载步骤

### 步骤1: 访问data.gov.hk网站

打开浏览器，访问: **https://data.gov.hk/tc/**

### 步骤2: 下载以下8个关键数据集

#### 2.1 HIBOR利率数据 (最重要!)
1. 访问: https://data.gov.hk/tc/dataset/hkma-hk-interbank-offered-rate
2. 点击"下载"按钮
3. 选择CSV格式下载
4. 保存到: `data_gov_hk_real/hibor_rates.csv`

#### 2.2 访客入境统计
1. 访问: https://data.gov.hk/tc/dataset/visitor-arrivals
2. 点击"下载"按钮
3. 选择CSV格式下载
4. 保存到: `data_gov_hk_real/visitor_arrivals.csv`

#### 2.3 实时交通数据
1. 访问: https://data.gov.hk/tc/dataset/hk-td-traffic-speed
2. 点击"下载"按钮
3. 选择CSV格式下载
4. 保存到: `data_gov_hk_real/traffic_speed.csv`

#### 2.4 MTR乘客数据
1. 访问: https://data.gov.hk/tc/dataset/mtr-passenger-ridership
2. 点击"下载"按钮
3. 选择CSV格式下载
4. 保存到: `data_gov_hk_real/mtr_passengers.csv`

#### 2.5 天气观测数据
1. 访问: https://data.gov.hk/tc/dataset/hko-weather-observations
2. 点击"下载"按钮
3. 选择CSV格式下载
4. 保存到: `data_gov_hk_real/weather_obs.csv`

#### 2.6 空气质量指数
1. 访问: https://data.gov.hk/tc/dataset/aqhi
2. 点击"下载"按钮
3. 选择JSON格式下载
4. 保存到: `data_gov_hk_real/aqhi.json`

#### 2.7 GDP数据
1. 访问: https://data.gov.hk/tc/dataset/gdp-statistics
2. 点击"下载"按钮
3. 选择CSV格式下载
4. 保存到: `data_gov_hk_real/gdp_data.csv`

#### 2.8 CPI数据
1. 访问: https://data.gov.hk/tc/dataset/cpi-statistics
2. 点击"下载"按钮
3. 选择CSV格式下载
4. 保存到: `data_gov_hk_real/cpi_data.csv`

---

## 📊 下载后的处理

### 步骤3: 创建真实数据集成脚本

下载完成后，运行以下命令集成数据:

```bash
python integrate_real_gov_data.py
```

这个脚本会:
1. 读取 `data_gov_hk_real/` 目录中的所有真实数据文件
2. 解析并标准化数据格式
3. 替换 `gov_crawler/data/` 中的模拟数据
4. 更新数据覆盖率统计

### 步骤4: 设置定时更新

创建每日更新的定时任务:

**Linux/Mac**:
```bash
# 编辑crontab
crontab -e

# 添加以下行 (每日上午9点更新)
0 9 * * * /usr/bin/python3 /path/to/scripts/update_gov_data.py
```

**Windows**:
1. 打开"任务计划程序"
2. 创建基本任务
3. 设置每日执行时间
4. 命令: `python C:\path\to\scripts\update_gov_data.py`

---

## 🎯 预期效果

### 数据覆盖率提升

| 数据源 | 当前状态 | 下载后状态 | 提升 |
|--------|----------|------------|------|
| HIBOR利率 | 0% (模拟) | 100% (真实) | +5指标 |
| 访客数据 | 0% (模拟) | 100% (真实) | +3指标 |
| 交通数据 | 0% (模拟) | 100% (真实) | +3指标 |
| MTR数据 | 0% (模拟) | 100% (真实) | +2指标 |
| 天气数据 | 0% (模拟) | 100% (真实) | +多指标 |
| 环境数据 | 0% (模拟) | 100% (真实) | +多指标 |
| GDP数据 | 0% (模拟) | 100% (真实) | +5指标 |
| CPI数据 | 0% (模拟) | 100% (真实) | +多指标 |

**总计提升**: 从31.4%提升到**80%+**

---

## 📁 目录结构

下载后，目录结构应该是:

```
project_root/
├── data_gov_hk_real/          # 新建的真实数据目录
│   ├── hibor_rates.csv        # HIBOR利率 (真实)
│   ├── visitor_arrivals.csv   # 访客数据 (真实)
│   ├── traffic_speed.csv      # 交通数据 (真实)
│   ├── mtr_passengers.csv     # MTR数据 (真实)
│   ├── weather_obs.csv        # 天气数据 (真实)
│   ├── aqhi.json             # 空气质量 (真实)
│   ├── gdp_data.csv          # GDP数据 (真实)
│   └── cpi_data.csv          # CPI数据 (真实)
├── gov_crawler/data/          # 现有模拟数据
│   ├── all_alternative_data_20251023_210419.json
│   └── backup_mock_20251106_003854.json  # 备份
└── scripts/                   # 定时更新脚本
    ├── update_gov_data.py
    └── cron_config.txt
```

---

## ⚠️ 注意事项

1. **数据格式**: 所有文件应该是CSV或JSON格式
2. **编码**: 确保使用UTF-8编码
3. **文件大小**: 每个文件可能几MB到几十MB
4. **下载速度**: 根据网络速度，可能需要几分钟
5. **更新频率**: 多数数据集每日或每月更新

---

## 🚀 下一步行动

**立即执行**:
1. [ ] 手动下载8个数据集
2. [ ] 运行 `integrate_real_gov_data.py` 整合数据
3. [ ] 设置定时更新任务

**预期结果**: 非价格数据真实覆盖率从31.4%提升到80%+

---

**状态**: 等待手动下载
**优先级**: 高
**预计时间**: 30分钟手动操作 + 10分钟自动化整合
