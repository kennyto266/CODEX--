# FRED API密钥申请指南

## 🎯 目标

申请FRED API密钥，将真实数据覆盖率从22.2%提升到**31.5%**

---

## 📊 FRED API数据价值

**联邦储备银行数据** - 宏观经济指标金标准

### 可获取的核心指标 (15+个)

| 指标类别 | 具体指标 | 数据价值 | 预计覆盖提升 |
|----------|----------|----------|--------------|
| **GDP数据** | GDP增长率、名义GDP、实际GDP | 经济基本面分析 | +2.5% |
| **通胀数据** | CPI通胀率、PPI、生产者价格 | 货币政策分析 | +2.0% |
| **就业数据** | 失业率、非农就业、劳动参与率 | 劳动力市场分析 | +2.0% |
| **利率数据** | 联邦基金利率、10年期国债收益率 | 利率环境分析 | +1.5% |
| **贸易数据** | 出口额、进口额、贸易差额 | 国际收支分析 | +1.0% |
| **工业数据** | 工业生产指数、产能利用率 | 工业景气分析 | +0.8% |

**总预计提升**: +9.3% (15/162)

---

## 🚀 申请步骤

### 步骤1: 访问申请页面

打开浏览器，访问：
```
https://fred.stlouisfed.org/docs/api/api_key.html
```

### 步骤2: 申请API密钥

1. **找到申请表单**
   - 页面会显示"Request API Key"按钮或表单
   - 如果未显示，向下滚动查找

2. **填写信息**
   ```
   Email Address: [输入您的邮箱]
   Reason for API Key: [输入用途说明]
   ```
   **建议填写**:
   ```
   Email: [您的邮箱地址]
   Purpose: Academic research on quantitative trading and economic indicators
   ```

3. **提交申请**
   - 点击"Request API Key"或"Submit"按钮
   - 系统会立即处理申请

### 步骤3: 接收API密钥

1. **检查邮箱** (立即或几分钟内)
   - 发件人: `api@federalreserve.gov` 或 `fred@stlouisfed.org`
   - 主题: `Your FRED API Key` 或类似

2. **保存API密钥**
   ```text
   API Key: abcdef1234567890abcdef1234567890
   ```
   - 将密钥保存在安全的地方
   - 建议保存到密码管理器

### 步骤4: 配置环境变量

**Windows (PowerShell)**:
```powershell
# 设置环境变量
$env:FRED_API_KEY = "your_api_key_here"

# 验证设置
echo $env:FRED_API_KEY
```

**Linux/Mac (Bash)**:
```bash
# 添加到 ~/.bashrc 或 ~/.zshrc
export FRED_API_KEY="your_api_key_here"

# 应用更改
source ~/.bashrc

# 验证
echo $FRED_API_KEY
```

---

## 📋 免费额度

| 项目 | 限制 | 备注 |
|------|------|------|
| **请求频率** | 120请求/分钟 | 非常慷慨的限制 |
| **每月请求数** | 无限制 | 按分钟限制，无总量限制 |
| **信用卡要求** | ❌ 不需要 | 完全免费 |
| **商业使用** | ✅ 允许 | 详见服务条款 |

---

## 🧪 测试API密钥

### 方法1: 使用curl

```bash
# 测试获取最新的GDP数据
curl "https://api.stlouisfed.org/fred/series/observations?series_id=GDPC1&api_key=YOUR_KEY&file_type=json&limit=1&sort_order=desc"

# 预期响应格式
{
  "observations": [
    {
      "date": "2024-01-01",
      "value": "26984.8"
    }
  ]
}
```

### 方法2: 使用Python测试脚本

创建 `test_fred_api.py`:

```python
#!/usr/bin/env python3
import requests
import os
import json

# 设置API密钥
FRED_API_KEY = os.environ.get('FRED_API_KEY')
if not FRED_API_KEY:
    print("请先设置FRED_API_KEY环境变量")
    exit(1)

# 测试获取GDP数据
url = "https://api.stlouisfed.org/fred/series/observations"
params = {
    'series_id': 'GDPC1',  # GDP
    'api_key': FRED_API_KEY,
    'file_type': 'json',
    'limit': 1,
    'sort_order': 'desc'
}

try:
    response = requests.get(url, params=params, timeout=10)
    response.raise_for_status()
    data = response.json()

    if 'observations' in data and data['observations']:
        latest = data['observations'][0]
        print(f"✅ FRED API测试成功!")
        print(f"指标: GDP (GDPC1)")
        print(f"日期: {latest['date']}")
        print(f"值: {latest['value']}")
    else:
        print("❌ API返回数据格式异常")

except Exception as e:
    print(f"❌ FRED API测试失败: {e}")
```

运行测试:
```bash
python test_fred_api.py
```

---

## 📈 集成到系统

### 更新适配器配置

1. **修改 `crypto_commodity_adapter.py`**
   ```python
   def __init__(self, config: Optional[Dict[str, Any]] = None):
       self.config = config or {}
       self.fred_api_key = os.environ.get('FRED_API_KEY')
       # 或从配置中获取
       # self.fred_api_key = self.config.get('fred_api_key')
   ```

2. **创建专门的FRED适配器** `fred_adapter.py`
   ```python
   class FredAdapter:
       def __init__(self):
           self.api_key = os.environ.get('FRED_API_KEY')
           self.base_url = "https://api.stlouisfed.org/fred"

       async def get_gdp_data(self):
           # 获取GDP数据
           pass

       async def get_inflation_data(self):
           # 获取通胀数据
           pass
   ```

---

## 🎯 覆盖提升计划

### 当前状态 (22.2%)
- 外汇汇率: 10个 ✅
- 美股: 5个 ✅
- 加密货币: 10个 ✅
- 港股: 3个 ⚠️

### 申请FRED后 (+9.3% → 31.5%)
- 新增: 宏观经济指标 15个
  - GDP相关: 3个
  - 通胀相关: 3个
  - 就业相关: 3个
  - 利率相关: 2个
  - 贸易相关: 2个
  - 工业相关: 2个

### 申请IEX Cloud后 (+6.2% → 37.7%)
- 新增: 高质量美股数据 10个

### 申请Finnhub后 (+4.9% → 42.6%)
- 新增: 全球股票数据 8个

---

## ⏱️ 时间估算

| 任务 | 预计时间 | 实际可能时间 |
|------|----------|--------------|
| 填写申请表格 | 2分钟 | 1-2分钟 |
| 接收API密钥 | 1分钟 | 1-5分钟 |
| 配置环境变量 | 3分钟 | 2-5分钟 |
| 测试API | 5分钟 | 5-10分钟 |
| 集成到系统 | 30分钟 | 30-60分钟 |
| **总计** | **45分钟** | **39-78分钟** |

---

## 🔗 有用链接

- **FRED API主页**: https://fred.stlouisfed.org/docs/api/
- **API密钥申请**: https://fred.stlouisfed.org/docs/api/api_key.html
- **数据系列搜索**: https://fred.stlouisfed.org/series/
- **API文档**: https://fred.stlouisfed.org/docs/api/fred/

---

## 💡 小贴士

1. **立即生效**: FRED API密钥通常在邮件发送后立即可用
2. **无需验证**: 不需要信用卡或身份验证
3. **速率友好**: 120请求/分钟非常宽松
4. **数据丰富**: 有数千个经济指标可选
5. **文档完善**: 官方文档非常详细

---

## ❗ 注意事项

1. **密钥安全**: 不要在代码中硬编码API密钥
2. **遵守限制**: 不要超过120请求/分钟
3. **数据引用**: 使用数据时请注明来源为FRED
4. **服务条款**: 遵守FRED API服务条款

---

## ✅ 检查清单

### 申请阶段
- [ ] 访问 https://fred.stlouisfed.org/docs/api/api_key.html
- [ ] 填写申请表单 (邮箱 + 用途)
- [ ] 提交申请
- [ ] 检查邮箱接收API密钥

### 配置阶段
- [ ] 设置FRED_API_KEY环境变量
- [ ] 运行测试脚本验证
- [ ] 检查API响应格式

### 集成阶段
- [ ] 更新适配器配置
- [ ] 测试获取经济数据
- [ ] 更新终极数据融合系统
- [ ] 重新计算覆盖率

### 验证阶段
- [ ] 确认覆盖率达到31.5%+
- [ ] 验证数据质量
- [ ] 检查错误日志

---

**预计完成时间**: 45分钟
**预计覆盖率提升**: +9.3% (22.2% → 31.5%)
**状态**: 🚀 准备开始申请
