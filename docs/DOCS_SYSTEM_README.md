# 文档测试和验证系统 (T599)

## 概述

文档测试和验证系统是一个全面的文档质量保证工具，用于检查文档的完整性、有效性、一致性和覆盖率。系统集成了质量检查、自动化测试、实时监控和CI/CD集成等功能。

## 核心功能

### 1. 📋 文档质量检查器 (`src/docs/quality_checker.py`)

**功能**:
- ✅ 检查文档完整性（缺失文件、空文件）
- ✅ 验证链接有效性（内部链接、外部链接）
- ✅ 检查拼写和语法（常见错误、大写规则）
- ✅ 统计文档覆盖率（代码文件 vs 文档文件）
- ✅ 验证代码与文档一致性（API版本、示例代码）

**检查项目**:
```
完整性检查
├── 必要文件 (README.md, API文档, 指南等)
└── 文件大小 (避免空文件或过小文件)

链接验证
├── 相对链接 (文档间跳转)
├── 外部链接 (http/https URL)
└── 断链检测 (自动验证可达性)

拼写检查
├── 常见拼写错误
├── 句子大写规则
└── 中文/英文混排

覆盖率统计
├── 代码文件总数
├── 已文档化文件
└── 覆盖率百分比

一致性验证
├── API文档同步
├── 版本号一致性
└── 示例代码有效性
```

**使用方法**:
```bash
# 单独运行质量检查
python src/docs/quality_checker.py . ./docs ./src

# 通过主运行器
python run_docs_system.py --command quality
```

### 2. 🧪 文档测试框架 (`src/docs/test_framework.py`)

**功能**:
- ✅ 自动发现文档中的代码块
- ✅ 执行代码块并验证输出
- ✅ 支持多种编程语言 (Python, Bash, JavaScript)
- ✅ 并行执行测试提高效率
- ✅ 详细的测试报告

**支持的测试类型**:
```
语法检查 (syntax)
├── Python AST 解析
├── Bash 语法验证
└── JavaScript 语法检查

执行测试 (execution)
├── 实际运行代码
├── 捕获标准输出
├── 检查退出代码
└── 验证期望输出

API测试 (api)
├── HTTP 请求验证
├── 响应状态检查
└── 数据格式验证

导入测试 (import)
├── Python 模块导入
├── 依赖检查
└── 环境验证
```

**代码块标记示例**:
```markdown
<!-- 标记为测试块 -->
```python {test}
import requests
response = requests.get('https://api.example.com')
assert response.status_code == 200
```

<!-- 跳过测试 -->
```python {skip}
print("这段代码不会执行")
```

<!-- 指定超时和期望输出 -->
```python {test, timeout=10, expected="Hello"}
print("Hello")
```

<!-- 仅语法检查 -->
```python
# 默认只做语法检查，不执行
def hello():
    print("Hello")
```
```

**使用方法**:
```bash
# 运行所有测试
python src/docs/test_framework.py ./docs ./src

# 指定测试类型
python src/docs/test_framework.py ./docs ./src --types syntax execution

# 指定并行工作线程
python src/docs/test_framework.py ./docs ./src --workers 8

# 保存结果
python src/docs/test_framework.py ./docs ./src --output test_results.json
```

### 3. 📊 监控系统 (`src/docs/monitor.py`)

**功能**:
- ✅ 实时监控文档质量指标
- ✅ 跟踪覆盖率趋势
- ✅ 记录问题历史
- ✅ 智能告警系统
- ✅ 生成趋势分析报告

**监控指标**:
```
覆盖率指标
├── 当前覆盖率百分比
├── 30天趋势 (改善/下降/稳定)
├── 平均值统计
└── 历史峰值记录

问题统计
├── 总问题数变化
├── 严重问题数变化
├── 分类问题统计
└── 问题解决率

链接健康
├── 断链数量
├── 链接总数
├── 断链率
└── 外部链接可用性

构建状态
├── 最后检查时间
├── 构建成功/失败
├── 执行时间
└── 构建频率
```

**告警阈值**:
- 文档覆盖率 < 70% → 警告
- 断链数量 > 5 → 错误
- 总问题数 > 20 → 错误
- 文档过期 > 30天 → 警告

**使用方法**:
```bash
# 更新监控数据
python src/docs/monitor.py update docs_quality_report.json

# 查看趋势分析
python src/docs/monitor.py trend 30

# 查看活跃告警
python src/docs/monitor.py alerts

# 导出所有数据
python src/docs/monitor.py export monitor_data.json
```

### 4. 🎨 监控仪表板 (`docs/dashboard.html`)

**功能**:
- 📈 实时图表展示
- 📊 关键指标监控
- 🚨 告警信息展示
- 📁 文件统计概览
- 🔄 自动刷新

**仪表板模块**:
```
概览卡片
├── 文档覆盖率 (状态指示)
├── 总问题数 (实时统计)
├── 断链数 (健康指标)
└── 总链接数 (完整性)

趋势图表
├── 覆盖率趋势图 (30天折线图)
├── 问题分布图 (饼图/环形图)
└── 历史对比 (多时间维度)

详细列表
├── 关键指标表格
├── 构建状态信息
├── 活跃告警列表
└── 文件统计网格
```

**访问方式**:
```bash
# 直接在浏览器中打开
open docs/dashboard.html

# 或通过本地服务器
python -m http.server 8000 -d docs
# 访问 http://localhost:8000/dashboard.html
```

### 5. 🚀 CI/CD 集成 (`.github/workflows/docs-test.yml`)

**自动执行**:
- ✅ Push到主分支时自动检查
- ✅ Pull Request时自动验证
- ✅ 每日定时检查 (凌晨2点)
- ✅ 支持手动触发

**工作流程**:
```
触发条件
├── 代码推送 (docs/, src/ 变更)
├── Pull Request 创建
├── 定时任务 (每日2:00)
└── 手动触发 (workflow_dispatch)

执行阶段
├── 1. 文档质量检查
│   ├── 完整性验证
│   ├── 链接检查
│   ├── 拼写检查
│   ├── 覆盖率统计
│   └── 一致性验证
│
├── 2. 文档测试
│   ├── 代码块发现
│   ├── 并行测试执行
│   ├── 结果收集
│   └── 报告生成
│
├── 3. 监控数据更新
│   ├── 指标更新
│   ├── 趋势分析
│   ├── 告警检查
│   └── 数据导出
│
├── 4. 外部链接检查 (定时任务)
│   ├── 链接收集
│   ├── 并行验证
│   └── 断链报告
│
└── 5. 覆盖率报告
    ├── 趋势图生成
    ├── 报告保存
    └── 数据归档
```

**质量门禁**:
- 严重问题数 > 0 → 阻止合并
- 总问题数 > 50 → 阻止合并
- 测试失败 > 0 → 阻止合并

**PR评论模板**:
```markdown
## 📋 文档质量检查报告

### 摘要
- 总问题数: **12**
- 严重问题: **3**
- 警告: **9**

> ⚠️ 发现问题，请查看完整报告。

检查时间: 2025-11-09 13:30:00
```

## 快速开始

### 1. 安装依赖

```bash
pip install requests beautifulsoup4 pyyaml
```

### 2. 运行完整检查

```bash
# 一键运行所有检查
python run_docs_system.py

# 详细输出
python run_docs_system.py --verbose

# 指定输出目录
python run_docs_system.py --output ./custom_reports
```

### 3. 查看结果

```bash
# 质量检查报告
cat docs/reports/quality_check_*.json

# 测试结果
cat docs/reports/test_results_*.json

# 监控数据
cat docs/reports/monitor_data.json

# 摘要报告
cat docs/reports/summary_report_*.md
```

### 4. 访问仪表板

```bash
# 启动本地服务器
cd docs
python -m http.server 8000

# 浏览器打开
open http://localhost:8000/dashboard.html
```

## 配置说明

### 监控阈值配置

编辑 `docs/.monitor/config.json`:

```json
{
  "refresh_interval": 300,
  "thresholds": {
    "min_coverage": 70,
    "max_broken_links": 5,
    "max_issues": 20
  },
  "charts": {
    "coverage_days": 30,
    "issues_days": 30
  }
}
```

### CI/CD 配置

编辑 `.github/workflows/docs-test.yml` 自定义:
- 超时时间
- 并行工作线程数
- 检查频率
- 告警通知

## 输出文件

### 报告文件

```
docs/reports/
├── quality_check_YYYYMMDD_HHMMSS.json  # 质量检查详细报告
├── test_results_YYYYMMDD_HHMMSS.json   # 测试结果报告
├── monitor_data.json                   # 监控数据
├── summary_report_YYYYMMDD_HHMMSS.md   # 摘要报告 (Markdown)
└── .monitor/
    ├── metrics.json                    # 当前指标
    ├── history.json                    # 历史数据
    ├── alerts.json                     # 告警记录
    └── config.json                     # 监控配置
```

### 数据格式

**质量检查报告**:
```json
{
  "timestamp": "2025-11-09T13:30:00",
  "summary": {
    "total_issues": 12,
    "critical_issues": 3,
    "warnings": 9,
    "categories": {
      "links": 5,
      "spelling": 4,
      "completeness": 2,
      "consistency": 1
    }
  },
  "details": {
    "links": {
      "total_links": 156,
      "broken_links": [
        {
          "file": "api_reference.md",
          "link": "https://example.com/doc",
          "type": "external",
          "error": "Unreachable"
        }
      ]
    },
    "coverage": {
      "code_files": 45,
      "documented_files": 35,
      "documentation_percentage": 77.8
    }
  }
}
```

**测试结果**:
```json
{
  "timestamp": "2025-11-09T13:30:00",
  "total_tests": 25,
  "summary": {
    "passed": 23,
    "failed": 2,
    "errors": 0,
    "skipped": 0
  },
  "tests": [
    {
      "file_path": "api_reference.md",
      "test_name": "api_reference_line_45",
      "status": "passed",
      "execution_time": 0.15,
      "output": "OK",
      "stdout": "...",
      "stderr": ""
    }
  ]
}
```

## 最佳实践

### 1. 文档编写

**代码块标记**:
```markdown
<!-- ✅ 好的示例 -->
```python {test}
import mymodule
result = mymodule.calculate(10, 20)
assert result == 30
```

<!-- ❌ 避免 -->
```python
# 缺少标记，无法测试
result = calculate(10, 20)
```

**链接使用**:
```markdown
<!-- ✅ 相对链接 (推荐) -->
[API参考](api/reference.md)

<!-- ✅ 绝对链接 -->
[GitHub](https://github.com/user/repo)

<!-- ❌ 避免断链 -->
[失效链接](nonexistent.md)
```

### 2. 定期维护

**每日**:
- 检查仪表板是否有新告警
- 修复报告中的问题

**每周**:
- 审查测试失败原因
- 更新文档覆盖率

**每月**:
- 分析趋势报告
- 优化文档结构

### 3. CI/CD 集成

**PR检查**:
```bash
# 在提交前本地验证
python run_docs_system.py --command all

# 检查通过后再提交
git add .
git commit -m "docs: update documentation"
git push
```

**失败处理**:
```bash
# 查看失败原因
cat docs/reports/summary_report_*.md

# 修复问题后重新运行
python run_docs_system.py --command quality

# 重新触发CI (提交空变更)
git commit --allow-empty -m "trigger ci"
```

## 故障排除

### 常见问题

**Q: 质量检查报告为空**
A: 检查文档路径是否正确，确保 `docs/` 目录存在且包含 `.md` 文件

**Q: 测试执行失败**
A: 检查代码块语法，确保 Python 环境正确安装依赖

**Q: 外部链接检查超时**
A: CI中限制了并发数量和超时时间，避免检查过多外部链接

**Q: 监控数据不更新**
A: 确保有写权限，或手动运行 `python src/docs/monitor.py update`

### 调试模式

```bash
# 启用详细日志
python run_docs_system.py --verbose

# 单独运行模块调试
python -c "
from src.docs.quality_checker import DocumentationQualityChecker
checker = DocumentationQualityChecker('.', './docs', './src')
results = checker.check_all()
print(json.dumps(results, indent=2))
"
```

## 扩展开发

### 添加新的检查器

```python
# 在 quality_checker.py 中添加方法
def _check_custom(self):
    custom_results = {...}
    self.results['details']['custom'] = custom_results
```

### 添加新的测试类型

```python
# 在 test_framework.py 中添加
def _run_custom_test(self, code: str, metadata: Dict, test: Dict):
    # 实现自定义测试逻辑
    return CodeTestResult(...)
```

### 自定义告警

```python
# 在 monitor.py 中修改阈值
self.thresholds['custom_metric'] = 100
```

## 性能优化

**并行执行**:
- 测试框架支持多线程并行执行
- 建议工作线程数 = CPU核心数

**缓存机制**:
- 监控数据自动缓存
- 避免重复检查相同文件

**批处理**:
- 外部链接检查限制并发数
- 大文件分批处理

## 参与贡献

欢迎提交 Issue 和 Pull Request！

1. Fork 项目
2. 创建功能分支
3. 提交变更
4. 发起 Pull Request

## 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 支持

如有问题，请提交 Issue 或联系维护团队。

---

**文档版本**: v1.0.0
**最后更新**: 2025-11-09
**维护者**: Claude Code Team
