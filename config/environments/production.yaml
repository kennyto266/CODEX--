# Production environment overrides

application:
  debug: false
  environment: production

api:
  reload: false
  workers: 4

logging:
  level: "WARNING"

database:
  pool_size: 20
  max_overflow: 30

trading:
  enabled: true

monitoring:
  enabled: true
