# 足球比分功能規格

## 概述

本規格定義了足球比分追蹤功能的詳細實現，重點關注香港馬會足球網站和國際足球賽事的比分追蹤。

## ADDED Requirements

### Requirement: System MUST scrape football scores from HKJC
系統必須從香港馬會足球網站爬取足球比分數據。

#### Scenario: 爬取香港足球比分
- **WHEN** 用戶查詢 `/score soccer` 時
- **THEN** 系統訪問香港馬會足球頁面並提取：
  - 香港超級聯賽比分
  - 香港甲組聯賽比分
  - 國際友誼賽比分
  - 亞洲杯、世界杯等大型賽事比分
  - 比分 (主隊和客隊)
  - 比賽時間 (上半場/下半場/補時)
  - 球隊名稱
  - 現場名稱

#### Scenario: 處理馬會網站動態內容
- **WHEN** 馬會網站使用動態加載時
- **THEN** 系統通過 Chrome MCP 執行：
  - 等待比分數據完全載入
  - 滾動頁面查看所有比賽
  - 點擊 "展開更多" 按鈕 (如果需要)
  - 等待 AJAX 請求完成
  - 提取所有可見比分

#### Scenario: 處理無比賽日
- **WHEN** 當日沒有足球比賽時
- **THEN** 系統返回：
  - "今日沒有足球比賽"
  - 顯示最近比賽日期
  - 提供明日或近日賽程查詢

### Requirement: System MUST support multiple football data sources
系統必須支持多個足球數據源以確保數據可靠性。

#### Scenario: 主要數據源 - 馬會
- **WHEN** 馬會網站可訪問時
- **THEN** 系統優先使用馬會數據：
  - 香港本地比賽準確性最高
  - 實時更新及時
  - 涵蓋主要國際賽事
  - 數據格式標準化

#### Scenario: 備用數據源 - ESPN足球
- **WHEN** 馬會網站不可用時
- **THEN** 系統切換到 ESPN足球：
  - URL: https://www.espn.com/soccer/scoreboard
  - 獲取全球足球比分
  - 篩選亞洲和香港相關比賽
  - 轉換為統一格式

#### Scenario: 第二備用 - FlashScore
- **WHEN** 前兩者都不可用時
- **THEN** 系統嘗試 FlashScore：
  - 備選方案
  - 僅在緊急情況下使用
  - 數據格式需要額外轉換
  - 記錄數據來源

#### Scenario: 數據源優先級
- **WHEN** 多個數據源可用時
- **THEN** 系統按以下優先級選擇：
  1. 馬會 (香港比賽)
  2. ESPN (國際比賽)
  3. FlashScore (備用)
  4. 最後緩存的數據

### Requirement: System MUST parse football match status
系統必須準確解析足球比賽狀態和時間信息。

#### Scenario: 解析比賽時間
- **WHEN** 獲取足球比賽狀態時
- **THEN** 系統識別：
  - `未開始` - 比賽尚未開始
    - 顯示開賽時間 (本地時間)
    - 顯示預計對戰
    - 顯示現場名稱
  - `上半場` - 比賽進行中，上半場
    - 顯示已過時間 (例如：23')
    - 顯示補時時間 (如果有)
    - 例如：23'+2 (23分鐘，補時2分鐘)
  - `下半場` - 比賽進行中，下半場
    - 顯示已過時間 (例如：67')
    - 顯示補時時間
    - 例如：67'+3
  - `中場休息` - 15分鐘中場休息
    - 顯示比分
    - 提示下半場開始時間
  - `已結束` - 比賽正式結束
    - 顯示最終比分
    - 顯示比賽時長

#### Scenario: 處理補時
- **WHEN** 比賽進入補時階段時
- **THEN** 系統執行：
  - 顯示補時分鐘數 (例如：90'+3)
  - 計算預計結束時間
  - 增加更新頻率 (每 30 秒)
  - 監聽傷停補時結束

#### Scenario: 處理加時賽
- **WHEN** 杯賽需要加時時
- **THEN** 系統識別：
  - 90分鐘比賽結束比分
  - 加時比分 (例如：加時 2-1)
  - 總比分 (例如：總比分 3-2)
  - 點球大戰 (如果適用)
  - 例如：點球 4-3

### Requirement: System MUST handle different leagues and competitions
系統必須處理各種足球聯賽和盃賽。

#### Scenario: 香港本地比賽
- **WHEN** 處理香港比賽時
- **THEN** 系統顯示：
  - 香港超級聯賽 (港超)
  - 香港甲組聯賽 (港甲)
  - 香港足總盃
  - 社區盃
  - 聯賽盃

#### Scenario: 國際比賽
- **WHEN** 處理國際比賽時
- **THEN** 系統顯示：
  - 世界杯 (決賽週和資格賽)
  - 亞洲杯
  - 歐洲盃
  - 國際友誼賽
  - 各大洲盃賽

#### Scenario: 聯賽分類
- **WHEN** 分類不同聯賽時
- **THEN** 系統按以下方式組織：
  - 按地區：香港、亞洲、歐洲、美洲、其他
  - 按級別：頂級聯賽、次級聯賽、青聯賽
  - 按類型：聯賽、盃賽、友誼賽

#### Scenario: 聯賽顏色標記
- **WHEN** 顯示聯賽信息時
- **THEN** 系統使用表情符號標記：
  - 🏆 世界杯/歐洲盃
  - 🥇 頂級聯賽 (英超、西甲等)
  - 🥈 次級聯賽
  - 🥉 地區聯賽
  - 🤝 友誼賽

### Requirement: System MUST format football score messages
系統必須將足球比分格式化為用戶友好的 Telegram 消息。

#### Scenario: 格式化當日足球比分
- **WHEN** 格式化多場足球比賽時
- **THEN** 系統生成以下格式的消息：

```
⚽ 足球比分 (2024-03-15)

🏆 香港超級聯賽
✅ 已結束
🥅 港足 2 - 1 傑志
   📅 19:30 | 現場: 香港大球場
   📊 觀眾: 8,532

🔴 進行中
⚡ 東方龍獅 vs 標準流浪 (下半場)
   💯 比分: 1 - 0
   ⏱️ 67'+2 | 🏟️ 旺角大球場
   📊 進度: 75%

⏸️ 即將開始
🕖 19:30 理文 vs 冠忠南區
   🏟️ 將軍澳運動場

🌏 國際賽事
✅ 已結束
🥅 曼城 3 - 1 利物浦
   📅 22:00 | 英超
   📊 觀眾: 54,000
   🎯 關鍵球員: Haaland (2球)

🔴 進行中
⚡ 皇馬 vs 巴塞隆拿 (上半場)
   💯 比分: 2 - 1
   ⏱️ 43'+1 | 🏟️ 班拿貝球場
   📊 進度: 55%

📅 明日賽程 (3月16日)
🕖 19:30 港足 vs 東方龍獅
🕘 21:00 曼聯 vs 阿仙奴
```

#### Scenario: 顯示單場比賽詳情
- **WHEN** 用戶點擊特定比賽或請求詳情時
- **THEN** 系統顯示：

```
🥅 港足 vs 傑志 (已結束)

🏆 最終比分
Hong Kong: 2
Kitchee: 1

⚽ 進球詳情
港足:
  • 12' 陳肇鈞 (助攻: 黃威)
  • 78' 安永佳 (十二碼)

傑志:
  • 34' 丹恩奴域 (助攻: 巴爾拿)

📊 比賽統計
控球率:
  港足: 52%
  傑志: 48%

射門:
  港足: 8 (4射正)
  傑志: 12 (5射正)

角球:
  港足: 6
  傑志: 7

黃牌:
  港足: 2
  傑志: 1

紅牌:
  無

⏰ 比賽時間: 94分鐘 (補時 4分鐘)
🎫 現場觀眾: 8,532
🏟️ 場地: 香港大球場
🌤️ 天氣: 晴天，25°C
👨‍⚖️ 球證: 廖國文
```

### Requirement: System MUST implement live football score updates
系統必須實現足球比賽的實時比分更新。

#### Scenario: 自動更新進行中的足球比賽
- **WHEN** 檢測到有足球比賽正在進行時
- **THEN** 系統執行：
  - 每 30 秒檢查比分變化
  - 如果進球，立即更新
  - 如果黃牌/紅牌，記錄但可能不立即通知
  - 中場休息時暫停更新
  - 補時階段增加更新頻率

#### Scenario: 進球通知策略
- **WHEN** 檢測到進球時
- **THEN** 系統可以：
  - 向所有查詢過該比賽的用戶發送通知
  - 顯示進球時間和進球球員
  - 更新比分和比賽狀態
  - 可選：發送簡短的進球回放描述

#### Scenario: 比賽階段更新頻率
- **WHEN** 管理更新頻率時
- **THEN** 系統執行：
  - 上半場/下半場：每 30 秒
  - 補時階段：每 15 秒
  - 傷停時間：每 10 秒
  - 中場休息：不更新
  - 比賽結束後 10 分鐘內：每分鐘檢查一次

### Requirement: System MUST handle football schedule
系統必須處理足球賽程信息。

#### Scenario: 獲取未來足球賽程
- **WHEN** 用戶執行 `/schedule soccer` 時
- **THEN** 系統提取：
  - 未來 7 天的所有足球比賽
  - 開賽時間 (本地時區)
  - 對戰隊伍
  - 聯賽/盃賽名稱
  - 現場名稱
  - 比賽重要性標記

#### Scenario: 賽程分類顯示
- **WHEN** 顯示足球賽程時
- **THEN** 系統按以下方式組織：
  - 按日期分組
  - 按聯賽分類
  - 標記重要比賽 (德比、決賽等)
  - 標記是否有直播

#### Scenario: 賽程變更通知
- **WHEN** 檢測到賽程變更時
- **THEN** 系統執行：
  - 識別延期、取消或時間變更
  - 通知相關用戶
  - 更新日曆和提醒
  - 記錄變更原因

### Requirement: System MUST support team favorites
系統必須支持用戶收藏足球隊。

#### Scenario: 添加球隊到收藏
- **WHEN** 用戶執行 `/favorite team_name` 時
- **THEN** 系統執行：
  - 驗證球隊是否存在
  - 添加到用戶收藏列表
  - 返回確認消息
  - 顯示收藏列表摘要

#### Scenario: 優先顯示收藏球隊
- **WHEN** 顯示比分時
- **THEN** 系統執行：
  - 將收藏球隊的比賽排在前面
  - 用特殊標記 (⭐) 標註
  - 提供快速跳轉按鈕
  - 顯示收藏球隊的比分摘要

#### Scenario: 管理收藏列表
- **WHEN** 用戶管理收藏時
- **THEN** 系統支持：
  - 查看收藏列表：`/favorites`
  - 移除收藏：`/unfavorite team_name`
  - 清空收藏：`/clear_favorites`
  - 分享收藏列表

### Requirement: System MUST handle timezone correctly
系統必須正確處理時區，特別是國際比賽。

#### Scenario: 轉換比賽時間到本地時區
- **WHEN** 顯示國際比賽時間時
- **THEN** 系統執行：
  - 檢測比賽的地區時區
  - 轉換到香港時間 (UTC+8)
  - 顯示原始時間和本地時間
  - 例如：22:00 GMT → 06:00 (次日)

#### Scenario: 夏令時處理
- **WHEN** 處理夏令時國家時
- **THEN** 系統執行：
  - 自動檢測夏令時狀態
  - 正確計算時差
  - 顯示明確的時區信息
  - 例如：20:00 BST (倫敦夏令時)

#### Scenario: 多時區顯示
- **WHEN** 顯示重要比賽時間時
- **THEN** 系統可以顯示：
  - 香港時間 (主要)
  - 比賽地點時間
  - UTC 時間 (可選)
  - 美東時間 (可選)

### Requirement: System MUST implement error handling for football data
系統必須實現足球特定的錯誤處理機制。

#### Scenario: 馬會網站維護
- **WHEN** 馬會網站進入維護模式時
- **THEN** 系統執行：
  - 檢測維護頁面
  - 切換到備用數據源
  - 記錄維護時間
  - 通知用戶數據來源變更

#### Scenario: 數據延遲
- **WHEN** 比分數據延遲時
- **THEN** 系統執行：
  - 檢測數據最後更新時間
  - 如果超過 2 分鐘，標記為 "可能延遲"
  - 嘗試其他數據源
  - 向用戶說明情況

#### Scenario: 比分不一致
- **WHEN** 不同數據源比分不一致時
- **THEN** 系統執行：
  - 比較所有可用的數據源
  - 選擇最可靠的來源
  - 標記數據為 "可能不準確"
  - 記錄不一致情況

## 數據模型

### FootballMatch
```python
@dataclass
class FootballMatch:
    match_id: str
    home_team: str
    away_team: str
    home_score: int
    away_score: int
    status: FootballMatchStatus
    minute: Optional[int] = None  # 比賽進行分鐘
    added_time: Optional[int] = None  # 補時分鐘
    competition: str  # 聯賽/盃賽名稱
    venue: Optional[str] = None
    attendance: Optional[int] = None
    start_time: datetime
    timezone: str = "UTC+8"
    last_update: datetime

    @property
    def is_live(self) -> bool:
        return self.status == FootballMatchStatus.LIVE

    @property
    def display_time(self) -> str:
        """返回顯示用時間"""
        if self.status == FootballMatchStatus.LIVE:
            if self.added_time:
                return f"{self.minute}'+{self.added_time}"
            return f"{self.minute}'"
        return self.start_time.strftime("%H:%M")
```

### FootballMatchStatus
```python
from enum import Enum

class FootballMatchStatus(Enum):
    SCHEDULED = "scheduled"
    LIVE = "live"  # 包括上半場/下半場
    HALFTIME = "halftime"
    FINISHED = "finished"
    POSTPONED = "postponed"
    CANCELLED = "cancelled"
```

### FootballCompetition
```python
@dataclass
class FootballCompetition:
    competition_id: str
    name: str  # 例如: "香港超級聯賽"
    short_name: str  # 例如: "港超"
    tier: int  # 1=頂級, 2=次級
    region: str  # 香港、亞洲、歐洲等
    emoji: str  # 表情符號
    importance: int  # 1-5, 5最重要
```

## 性能要求

- **頁面加載時間**: < 5 秒 (馬會網站)
- **數據解析時間**: < 1.5 秒
- **更新頻率**: 每 30 秒 (進行中比賽)
- **緩存命中率**: > 70%
- **成功率**: > 95%
- **並發處理**: 支持 15 個並發請求

## 監控指標

1. 馬會網站可訪問性
2. ESPN足球可訪問性
3. 數據源切換頻率
4. 比賽狀態變更檢測
5. 進球通知成功率
6. 用戶查詢頻率
7. 收藏功能使用率
8. 時區轉換準確性

## 限頻策略

- 每個 IP 每分鐘最多 20 次請求
- 每個用戶每分鐘最多 5 次查詢
- 爬蟲間隔：至少 20 秒
- 進球通知：每場比賽最多每分鐘 1 次

## 相關規格

- `specs/sports-scoring` - 體育比分核心框架
- `specs/telegram-bot` - Telegram Bot 集成
- `specs/web-scraping` - 網頁爬取基礎
- `specs/sports-schedule` - 賽程查詢功能
